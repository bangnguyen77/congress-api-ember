import { partyName } from 'congress-api/helpers/party-name';
import { module, test } from 'qunit';

module('Unit | Helper | party name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = partyName([42]);
  assert.ok(result);
});
