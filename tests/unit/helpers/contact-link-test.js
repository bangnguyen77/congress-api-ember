import { contactLink } from 'congress-api/helpers/contact-link';
import { module, test } from 'qunit';

module('Unit | Helper | contact link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = contactLink([42]);
  assert.ok(result);
});
